# 軽量dotfiles管理システム 実装仕様書 (Rev.3)

## 1. 背景と設計思想

既存のdotfiles管理ツールには、(a) ツール自体の導入が前提となり新環境での導入順序問題が生じる、(b) シンボリックリンクか物理コピーかをツール側が強制し運用が硬直する、(c) テンプレート機能を持つ一部ツールがリポジトリ内のファイル名を変更するため、エディタのシンタックスハイライトや補完が効かなくなる、という構造的課題がある。

本システムはこれらをGit標準機能（filter driver / include / attributes）と小さなシェルスクリプトのみで解決する。シェルスクリプトは可能な限りPOSIX範囲で記述し、POSIXで賄えない箇所のみ bash / zsh のいずれでも、また GNU / BSD いずれのコアユーティリティ環境でも動く書き方を採用する。独自バイナリやランタイムを持たず、管理システム自体をdotfilesリポジトリに同梱できるため、`git clone` 直後から追加導入なしに利用できる。リポジトリ内のファイル名は配置先と同一に保たれ、配置方式（コピー、リンク、その他）はユーザーが `dotfiles.deployCmd` で任意に指定する。実装は小さなスクリプトとGit設定のみで完結し、ユーザー自身による機能拡張を容易にしている。

## 2. 構成と設定定義

システムの設定は `dotfiles.gitconfig` に集約される。ユーザーはこのファイルをGitのローカル設定として読み込むことで、フィルタ定義やエイリアスを利用可能にする。システムのエントリーポイントはリポジトリルートのシェルスクリプト `dotfiles.sh` とし、エイリアスを通じて呼び出される。システムが利用する外部コマンドはconfig内で定義し、疎結合を保つ。具体的には、外部シークレットストアから値を取得する `dotfiles.fetchCmd` と、リポジトリからシステムへファイルを配置する `dotfiles.deployCmd` をユーザーが自由に定義する。

最小例：

```ini
[filter "dotfiles-filter"]
    smudge = ./dotfiles.sh smudge %f
    clean  = ./dotfiles.sh clean  %f
    required = true

[alias]
    dotfiles = "!f() { ./dotfiles.sh \"$@\"; }; f"

[dotfiles]
    deployCmd = cp -Lp -vf
    fetchCmd  = bw-fetch

[dotfiles "Job/.gitconfig"]
    pathDarwin       = ~/Projects/Job/.gitconfig
    argsForUSERNAME  = "Projects/Job/.gitconfig user_name"
    argsForUSEREMAIL = "Projects/Job/.gitconfig user_email"
```

これにより `git dotfiles deploy`、`git dotfiles capture`、`git dotfiles ls` のようにスペース区切りのサブコマンドとして呼び出せる。

### セットアップ

`dotfiles.gitconfig` は対象リポジトリのローカル設定として読み込ませる。`git clone` 直後に以下を一度実行する。

```sh
git config --local include.path './../dotfiles.gitconfig'
```

これによりフィルタドライバ、エイリアス、`dotfiles.*` 設定値がすべて有効になる。グローバル設定を汚さず、リポジトリごとに完結する。

### ファイル定義と配置先の解決

管理対象ファイルはリポジトリ内の相対パスをキーとして定義される。配置先はデフォルトで `$HOME` 起点の同名パスとなるが、config内の `path` 属性により任意の配置先を指定可能である。OS間の差異を吸収するため、配置先は以下の順序で解決される。

1. `pathLinux` や `pathDarwin` といったOS固有のパス設定。
2. 基本設定である `path`。
3. 設定がない場合のデフォルトパス（`$HOME` 起点の同名パス）。

### プレースホルダーと引数の正規化

ファイル内の秘匿情報は、実ファイル内では `%%KEY%%` という形式で記述する。Git configの構文制約を回避するため、外部コマンドへの引数定義キーは、プレースホルダーから記号を除去し英数字のみに正規化した文字列を使用する。たとえば `%%API-KEY%%` は `argsForAPIKEY` にマッピングされる。ここでも `argsLinuxForAPIKEY` のように `args` と `For` の間にOS名を挟んだキーを優先して評価し、環境ごとの外部ストアの差異を吸収する。

### `fetchCmd` の契約

`fetchCmd` は外部シークレットストアから値を取得するユーザー定義コマンドである。本システムは以下の規約で呼び出す。

- **引数**：`argsForXXX` の値をシェルの語分割規則（クォート尊重、`eval set --` 相当）でトークン化し、`fetchCmd` の引数として渡す。たとえば `argsForUSERNAME = "Projects/Job/.gitconfig user_name"` は `fetchCmd "Projects/Job/.gitconfig" "user_name"` として呼ばれる。
- **stdout**：シークレットの値を出力する。末尾の改行はすべて除去される（POSIX `$(...)` 同様）。途中の改行は保持し、複数行のシークレット（PEM鍵等）も許容する。末尾の非改行空白は保持する。改行コードは LF とし、CRLF 等を出力してはならない。Git は Smudge フィルタの出力に対して EOL 変換を後段で適用するため、`fetchCmd` が CRLF を返した場合でも作業ツリーに書かれる値は LF になりうる。このとき Clean フィルタが `fetchCmd`（CRLF）の返り値で作業ツリー（LF）を検索してもマッチせず、逆置換が失敗する。
- **stderr**：そのままユーザーの端末へ通す（認証プロンプト等が見えるように）。
- **終了コード**：非ゼロは失敗を意味する。Smudge プロセスは即座に非ゼロ終了し、`required = true` により `git checkout` 等の Git 操作自体が中断する。Git は `required` フィルタの出力をコマンド正常終了後に確定するため、途中失敗による作業ツリーの半端な書き出しは発生しない。
- **呼び出し回数**：1ファイル中に同一の `%%KEY%%` が複数回出現する場合、各出現ごとに呼び出す（メモ化しない）。

### `deployCmd` の契約

`deployCmd` はリポジトリ内のファイルをシステムへ配置するユーザー定義コマンドである。本システムは以下の規約で呼び出す。

- **引数**：`deployCmd` の値をシェルの語分割規則（`fetchCmd` と同じ）でトークン化したうえで、末尾に2つの引数 `<SRC> <DST>` を**システム側が自動で付与**して実行する。`SRC` はリポジトリルートからの相対パス、`DST` は §2「ファイル定義と配置先の解決」で求まる絶対パス。たとえば `deployCmd = cp -Lp -vf` と定義されていれば、実際の呼び出しは `cp -Lp -vf Job/.gitconfig /Users/me/Projects/Job/.gitconfig` となる。ユーザーは設定値の中で `<SRC>`/`<DST>` を明示する必要はない。
- **stdout / stderr**：そのままユーザーの端末へ通す（`cp -v` 等の進捗を見せるため）。
- **終了コード**：非ゼロは失敗。`deploy` コマンドは当該ファイルを失敗扱いとし、残りのファイルの処理を継続したうえで、最終的に非ゼロで終了する。

## 3. フィルタエンジンの挙動

`dotfiles-filter` ドライバーが、Gitのフィルタ機能を通じて状態変換を行う。安全性を担保するため、Gitの `required` オプションを有効化し、処理失敗時はGitの操作自体をアボートさせる。

### Smudgeプロセス（Repo → WorkTree）

`git checkout` 等のタイミングで実行される。ファイル内の各 `%%KEY%%` について、対応する `argsForXXX`（OS固有キーが優先）を解決し、§2「`fetchCmd` の契約」に従ってシークレットを取得する。取得した実値で `%%KEY%%` を置換し、作業ツリーへ書き出す。`fetchCmd` の失敗は smudge 全体の失敗として伝播する。

### Cleanプロセス（WorkTree → Repo）

`git add` 等のタイミングで実行される。Smudge と対称な処理として、対象ファイルに紐づく `[dotfiles "FILE"]` セクション配下の `argsForXXX` 定義をすべて列挙し、各キーについて §2「`fetchCmd` の契約」に従って実値を取得する。取得した実値が作業ツリーのファイル中に出現していれば、その文字列を `%%KEY%%` に置換してステージングへ書き出す。当該ファイルに紐づく `argsForXXX` が1つも定義されていない場合、Clean は何も置換せず入力をそのままステージングへ書き出す（恒等変換）。

逆置換が成立しなかったキー（実値がファイル中に1度も現れなかったもの）はエラーにせず、当該キー名を stderr に警告として列挙する。ユーザーがそのキーを使わない構成にした場合や、キーをまだ参照に組み込んでいない過渡期で誤検知しないためである。

`fetchCmd` 自体の失敗（非ゼロ終了）は Clean 全体の失敗として伝播し、`required = true` により `git add` が中断される。

## 4. 属性ベースの宣言的制御

デプロイ対象の制御にはGit標準の `.gitattributes` を使用する。

最小例：

```gitattributes
Job/.gitconfig    filter=dotfiles-filter dotfile=darwin
.ssh              chmod=700
.ssh/**           chmod=600
```

### OSフィルタリング

`dotfile` 属性が付与されたファイルのみが処理対象となる。属性値の解釈は以下の通り。

- **値なし（`set` 状態、`dotfile` とだけ書く）**：すべての実行環境で対象
- **OS識別子の単一指定**：`dotfile=darwin` のように1つのOS識別子（`linux`, `darwin` 等、`uname` の小文字化結果）を指定。実行環境と一致する場合のみ対象
- **OS識別子のカンマ区切りリスト**：`dotfile=linux,darwin` のように複数のOS識別子をカンマで区切って指定。実行環境がリスト中のいずれかに一致する場合に対象
- **未定義（`unset` 状態を含む）**：管理対象外としてスキップ

### 権限の適用

`.gitattributes` 内に `chmod` 属性を定義することで、ファイルのパーミッションを宣言的に指定する。属性値は POSIX `chmod` コマンドが受け付ける書式すべてを許容し、内部では `chmod -- <value> <path>` の形で適用する（先頭が `-` のシンボリックモードを安全に渡すため `--` を挟む）。8進数（`0600`）でもシンボリック（`u+rw,go=`）でも記述可能だが、実用上は8進数で十分なケースが多い。

ファイルとディレクトリの両方について、`chmod` 属性はファイルと同じ `.gitattributes` パターンマッチで解決する（`git check-attr chmod <path>` 相当）。Git はトラック対象外のパスでも属性を評価できるため、`.ssh chmod=700` のようなエントリはディレクトリパスへの指定として機能する。

デプロイ処理において、対象ファイルの親ディレクトリが存在せずに新規作成する際、そのディレクトリパスに対して解決された `chmod` 属性を適用する。その後、ファイルのデプロイが完了した直後に、ファイル自身に対する `chmod` 属性を適用し、厳格な権限管理を実現する。

## 5. コマンドインターフェース

ファイルの編集やコミットなどの基本操作は通常のGitコマンドを使用する。独自の操作はGitエイリアスとして実装される以下のヘルパーコマンドを通じて行う。

### deploy

`dotfile` 属性が付与されているファイルを対象に、ユーザーが定義した `deployCmd` を実行してシステムへデプロイする。

- **対象選択**：引数なしで実行した場合は OS フィルタを通過した全ファイル、引数にリポジトリ相対パス（SRC、複数可）を指定した場合は当該 SRC のみを対象とする（例：`git dotfiles deploy Job/.gitconfig`）。引数で指定した SRC が `dotfile` 属性を持たない場合はエラーとして処理を中止する。
- **処理順序**：各対象ファイルについて、(1) 配置先の親ディレクトリが存在しなければ作成し、解決された `chmod` 属性をディレクトリに適用、(2) `deployCmd` を §2「`deployCmd` の契約」に従って実行、(3) 配置完了後にファイル自身へ `chmod` 属性を適用する。`chmod` をデプロイ後に適用するのは、`cp` 等の配置コマンドがパーミッションを上書きする可能性があるため。
- **上書きポリシー**：システム側で上書き可否を決めない。`deployCmd` の挙動に委ねる（例：`cp -Lp -vf` で無条件上書き、`cp -Lpn` で既存ファイル保持）。
- **失敗時**：1ファイルの失敗で全体を停止せず、残りのファイルの処理を続行する。最終的に1件でも失敗があれば非ゼロで終了し、失敗したファイルのリストを stderr に出力する。
- **`--dry-run` は持たない**：配置対象と配置先は `ls` サブコマンドで確認できる。

### capture

システム上のファイル（DST）を、作業ツリーのリポジトリ内パス（SRC）へ取り込むコマンド。

- **対象指定**：1つ以上の DST パスを引数で必須指定する。指定された各 DST について以下の処理を順に行う。
- **SRC の決定**：DST から SRC を以下の順序で解決する。
  1. **既管理ファイル**：DST が既存の `[dotfiles "FILE"]` の `path*` 解決結果と一致する場合、その `FILE`（リポジトリ相対パス）を SRC とする。逆引きは全管理ファイルに対する線形検索でよい。
  2. **未管理だが `$HOME` 配下**：DST を `realpath` で正規化したパスが `$HOME` の前方一致となる場合、`$HOME` を剥がした残りの相対パスを SRC とする（例：`~/.gitconfig` → `.gitconfig`）。
  3. **未管理かつ `$HOME` 外**：エラーとして処理を中止し、ユーザーに `dotfiles.gitconfig` への `[dotfiles "FILE"]` セクションの事前定義を促す。`$HOME` 外への自動セクション生成は行わない。
- **同一性判定**：SRC が決まったうえで `[ "$DST" -ef "$SRC" ]` によりデバイスIDと inode の一致を検査し、同一ファイル（シンボリックリンクでDSTがSRCを指している運用等を含む）と判定された場合は何もしない。
- **取り込み**：同一でない場合のみ `cp -vf <DST> <SRC>` で強制上書きコピーする。`git add` までは実行せず、ステージングは通常の Git ワークフローに任せる。これにより、Clean フィルタによる `%%KEY%%` への巻き戻しが意図せず発生することを防ぐ。
- **`dotfile` 属性の自動付与**：取り込んだファイルにまだ `dotfile` 属性が付与されていない場合、リポジトリルート直下の `.gitattributes` の末尾に `SRC dotfile` の行を追記する（属性値なし＝ `set` 状態）。OS 別の運用を始めたければユーザーが手で書き換える。追記する行に `filter=dotfiles-filter` は含まない。ファイル内に秘匿情報の置換が必要な場合は、`git add` を行う前にユーザーが手動で `filter=dotfiles-filter` を `.gitattributes` に追記すること。

### ls

管理対象ファイルの一覧を表示する。OS設定および属性フィルタに基づき、1ファイルにつき1行、`<SRC> -> <DST>\n` の形式で stdout に出力する。`SRC` はリポジトリルートからの相対パス、`DST` は §2 で解決された配置先の絶対パス。

## 6. 既知の制約

- **文字列衝突**：Clean プロセスの逆置換は構文解析を行わず単純な文字列置換を採用しているため、シークレットと同一の文字列が偶然ファイル内の別箇所に存在する場合、それらも一律にプレースホルダー化される。通常、シークレットは短すぎず不規則かつ複雑であるため、実運用上で問題になることは稀である。
- **Windowsネイティブ非対応**：`uname` による OS 判定および `cp` / `chmod` / `realpath` 等の POSIX コマンドに依存するため、Windows のネイティブシェル環境（cmd, PowerShell）では動作しない。WSL や Git for Windows の bash 環境では動作する可能性がある。
- **バイナリファイル非対応**：Smudge / Clean は単純な文字列置換を行うため、バイナリファイルに `filter=dotfiles-filter` を付与すると、`%%KEY%%` のバイト列が偶然マッチしたり、シークレットに含まれるNULバイト等の扱いで内容が破損する可能性がある。バイナリファイルにはフィルタを付与しない運用が前提である。
